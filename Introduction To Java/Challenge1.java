public class Challenge1 {
    public static void main(String[] args) {

// 1. Create two integer variables length and breadth and assign then some values.
// 2. Create a double variable "perimeter" to calculate the perimeter of a rectangle.

// 3. Task1 : Create two integer variables length and breadth and assign then some values.
//    Task2 : Create a double variable "perimeter" to calculate the perimeter of a rectangle.

// Solution 1 ->

        int length = 25;
        int breadth = 10;
        int areaOfRectangle = length * breadth;
        System.out.println("Area of a square = " + areaOfRectangle );

// Solution 2 ->

        double length1 = 44;
        double breadth1 = 30;
        double perimeterOfRectangle = 2 * (length1 + breadth1);
        System.out.println("The perimeter of a rectangle = " + perimeterOfRectangle);

// Solution 3 ->

        int length3 = 20;
        int breadth3 = 15;
        double perimeter = 2 * (length3 + breadth3);
        System.out.println("The perimeter of a rectangle is = " + perimeter);
    }
}
